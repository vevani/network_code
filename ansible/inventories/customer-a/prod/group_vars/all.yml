---
# Variables applied to all hosts — prod environment
# Sensitive values should be stored in Ansible Vault or an external secrets manager.

ansible_diff_mode: false
environment_name: prod
customer_name: customer-a
